# 五子棋 AI 对弈 - 设计说明

## 项目概述

一个纯前端实现的五子棋游戏，用户可以配置兼容 Anthropic 格式的 API，与真正的 AI 大语言模型进行对弈。

---

## 界面布局

整体采用**左右分栏布局**，简约清晰：

```
┌─────────────────────────────────┬──────────────────┐
│ 标题：五子棋 AI 对弈             │                  │
├─────────────────────────────────┤                  │
│                                 │  API 配置面板    │
│          游戏区域               │  [Base URL输入]  │
│                                 │  [API Key输入]   │
│    ┌─────────────────────────┐  │  [Model输入]     │
│    │       15×15 棋盘        │  │                  │
│    │                         │  │  [保存开始按钮]  │
│    └─────────────────────────┘  │                  │
│                                 │   规则说明面板   │
│ 状态：轮到你落子 / AI 思考中    │                  │
│          [重新开始按钮]         │                  │
└─────────────────────────────────┴──────────────────┘
```

---

## 界面详细说明

### 左侧游戏区域

| 区域 | 内容 | 说明 |
|------|------|------|
| **状态栏** | 文字提示 | 显示当前游戏状态：`请配置 API 后开始` / `轮到你落子` / `AI 思考中` / `你赢了！` / `AI 赢了！` / `平局` |
| **棋盘** | 15×15 网格 | - 温暖木纹背景<br>- 黑色网格线标出交叉点<br>- 天元位置(7,7)有小圆点标记<br>- 用户执黑（纯黑棋子），AI 执白（纯白棋子）<br>- 棋子带轻微阴影，有立体感<br>- 落子时有缩放弹入动画 |
| **按钮** | 重新开始 | 游戏开始后可点击，清空棋盘重新开始 |

### 右侧侧边栏

| 区域 | 内容 | 说明 |
|------|------|------|
| **API Base URL** | 输入框 | 默认值：`https://api.anthropic.com/v1/messages`<br>支持修改为任何兼容 Anthropic 格式的 API 地址 |
| **API Key** | 密码输入框 | 用户填写自己的 API 密钥，输入时隐藏 |
| **Model** | 输入框 | 默认值：`claude-3-sonnet-20240229`<br>可修改为你需要的模型名称 |
| **按钮** | 保存配置并开始 | 点击后保存配置到浏览器本地存储，初始化游戏 |
| **规则说明** | 文本列表 | 简单说明游戏规则 |

---

## 用户操作流程

```
1. 打开页面
   ↓
2. 输入 API Key（Base URL 和 Model 已有默认值可直接用）
   ↓
3. 点击「保存配置并开始」
   ↓
4. 配置自动保存到浏览器 localStorage，下次打开无需重输
   ↓
5. 游戏开始 → 用户（黑子）先行
   ↓
6. 用户点击棋盘空位落子
   ↓
7. 检查胜负：
      ✓ 用户连成五子 → 显示「你赢了」→ 结束
      ✗ 未连成 → 继续
   ↓
8. AI 回合：
      - 显示蒙层 + 加载动画 + "AI 思考中"
      - 禁用棋盘点击，防止重复操作
      - 将完整棋盘状态发送给 API
      - AI 返回坐标 → AI 落子
   ↓
9. 检查胜负：
      ✓ AI 连成五子 → 显示「AI 赢了」→ 结束
      ✗ 未连成 → 返回步骤 6 等待用户
```

---

## 技术特点

- **纯前端实现**：不需要后端服务器，浏览器直接打开即可运行
- **兼容任意 Anthropic 格式 API**：官方 Claude / 本地开源模型 / 第三方服务商都支持
- **真 AI 决策**：每次都将完整棋盘状态发给 AI，由 AI 计算下一步，而非预设算法
- **配置本地持久化**：用户的 API 配置保存在浏览器本地，不会上传到任何服务器
- **响应式设计**：手机、平板、桌面都能正常游玩
- **简约美观**：木纹棋盘 + 干净棋子 + 优雅动画

---

## 文件结构

```
my-project/
├── index.html    # 主页面
├── styles.css    # 样式文件
├── game.js       # 游戏核心逻辑（棋盘、胜负判断）
├── api.js        # API 调用封装（兼容 Anthropic 格式）
├── app.js        # 主应用交互逻辑
└── 设计说明.md   # 本文档
```

---

## AI Prompt 格式

每次 AI 回合会发送类似这样的提示词：

```
你现在正在和玩家进行五子棋对弈。
规则：连成五个相同颜色的棋子即获胜。棋盘是15x15，坐标从(0,0)到(14,14)。
当前棋盘状态：
- 黑子（玩家）用 X 表示
- 白子（你）用 O 表示
- 空位用 . 表示

棋盘：
...............
...............
...............
...X...........
....O..........
...............
.......X.......
...............
...O...........
...............
...............
...............
...............
...............
...............

你执白子，现在请你输出下一步落子的坐标，格式为 "row,col"，例如 "7,7"。只输出坐标，不要输出其他内容。
```
